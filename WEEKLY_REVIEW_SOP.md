# Agent 质量周评 SOP

> **目标用户**：内部产品、研发工程师
> **文档定位**：操作手册，不讲原理只讲怎么做
> **适用平台**：AgentTrace（大盘 / 明细 / 异常 三大菜单）

---

## 本周目标（OKR）

### O · Objective

**用 5 天时间，将平台沉淀的 session 数据转化为下周 Agent 优化的可执行输入，形成"评估 → 归因 → 改进 → 验证"的闭环。**

### KR · Key Results

| 编号 | 关键结果 | 衡量口径 |
|------|---------|---------|
| KR1 | 完成本周全部 session 的规则扫描，覆盖率 100% | 大盘 / 异常页统计的 session 数 = 数据源原始数 |
| KR2 | 完成约 100 条 session 的 LLM 语义评判，规则与 LLM 偏差 ≥ 15 分的 case 100% 进入人工复核 | LLM 跑批日志 + 人工复核清单交叉核验 |
| KR3 | 产出本周「Agent 优化 Action 清单」≥ 5 条，其中 P0 / P1 ≥ 2 条 | 周评报告中 P0 / P1 条目数 |
| KR4 | 上周输出的 P0 / P1 Action，本周指标验证完成率 100% | 周评报告"验证方式"列对应的指标变化 |

### 本周三大产出物

#### 产出物 1 · 问题分布表（阶段一交付）

- **目的**：用 5 分钟向 Leader 回答"本周整体怎么样、哪类问题在变多"
- **来源**：阶段一规则扫描
- **内容**：
  - 每种异常类型的命中数、占总量比例、周环比趋势
  - 5 维健康度雷达截图 + 四档分布
  - 核心指标快照（P95 / P50 / 总成本 / 累计 Token）
  - 初判优先级（P0 / P1 / P2）
- **使用场景**：周一晨会、月度复盘的输入
- **存放位置**：飞书文档「Agent 周评 / 第 N 周 / 问题分布表」

#### 产出物 2 · 问题清单（阶段二交付）

- **目的**：定位规则覆盖不到的语义级问题，找到规则迭代方向
- **来源**：阶段二 LLM-as-Judge 跑批结果
- **内容**：每条 session 一行
  - Session ID
  - 规则健康度 / LLM 综合分 / 偏差
  - 主要问题（一句话）
  - 根因分类（Prompt / 工具 / 规划 / 环境 四选一）
  - LLM 给出的改进建议
- **使用场景**：阶段三人工复核的工作台、规则迭代的素材库
- **存放位置**：飞书表格「Agent 周评 / 第 N 周 / LLM 评判明细」

#### 产出物 3 · 周评报告 · Action 清单（阶段三交付，本周最终交付物）

- **目的**：给研发 / 算法明确的、可执行的、可验证的优化任务
- **来源**：阶段三人工校准结果
- **内容**：每条 Action 必须包含 7 项
  | 字段 | 必填 | 说明 |
  |------|-----|------|
  | 优先级 | ✅ | P0（本周必做）/ P1（下周迭代）/ P2（观察项） |
  | 问题描述 | ✅ | 一句话说清楚现象 |
  | 影响面 | ✅ | 命中 session 数 + 占总量 % |
  | 根因 | ✅ | Prompt / 工具 / 规划 / 环境 四选一 |
  | 优化动作 | ✅ | 具体到"改哪个文件、加什么约束、调哪个参数" |
  | 负责人 | ✅ | @单一责任人，避免"谁都能做" |
  | 验证方式 | ✅ | 下周看哪个指标的变化、达到什么阈值算通过 |
- **使用场景**：周五周会评审 → 进入下周迭代排期 → 下周评审会上验证
- **存放位置**：飞书文档「Agent 周评 / 第 N 周 / 周评报告」+ 同步 Meego 任务

### 本周不做的事（Non-goals）

为避免 SOP 跑偏，明确**本周不做**：

- ❌ 不做规则评分公式的迭代（独立排期，避免和评估流程耦合）
- ❌ 不做平台功能开发（如新增大盘卡片），评估流程只用现有能力
- ❌ 不做跨周趋势分析（单周内数据样本不足以下结论，至少累积 4 周后再做）

---

## 整体流程

```
全量 Session（约 500 条/周）
    │
    ▼
┌─────────────────────────────┐
│ 阶段一：规则扫描（Day 1-2）   │  全量，零成本
│ 输出：问题分布表             │
└───────────┬─────────────────┘
            │ 按"是否需要 LLM 裁判"筛选
            ▼
┌─────────────────────────────┐
│ 阶段二：LLM-as-Judge（Day 2-4）│  约 100 条，语义分析
│ 输出：问题清单 + 偏差表      │
└───────────┬─────────────────┘
            │ 按"是否需要人工定性"精筛
            ▼
┌─────────────────────────────┐
│ 阶段三：人工校准（Day 4-5）   │  约 30 条，定性根因
│ 输出：周评报告（Action 表）  │
└─────────────────────────────┘
```

---

## 阶段一 · 规则扫描（Day 1-2）

**目标**：用平台已有规则引擎，对全量 session 做健康度扫描，输出宏观问题分布。

### 操作步骤

**1. 打开「大盘」页**，记录：

- 5 维雷达中得分最低的维度（思考 / 编排 / 稳定 / 响应 / 资源）
- 四档健康度分布比例（健康 ≥85 / 一般 70-84 / 风险 50-69 / 严重 <50）
- 核心指标当前值：P95、P50、总成本、累计 Token

**2. 打开「异常」页**，按命中数降序排列，记录：

- 每种异常标签的命中 session 数
- 趋势 sparkline 是否上升（上升 = "正在变多"，标记为高优先）
- 平均健康度最低的聚类

**3. 输出物：问题分布表**

| 异常类型 | 命中数 | 占总量 % | 周趋势 | 平均健康度 | 初判优先级 |
|---------|--------|---------|--------|-----------|-----------|
| 轨迹异常（无答复） | 42 | 18% | ↑ | 38 | P0 |
| 出现死循环 | 27 | 12% | → | 45 | P1 |
| 执行失败 | 15 | 6% | ↓ | 52 | P2 |
| 上下文超限 | 8 | 3% | → | 61 | P2 |

> 该表会在阶段三补全"根因 / 优化动作 / 负责人"列。

---

## 阶段二 · LLM-as-Judge（Day 2-4）

**目标**：对规则无法判定的"灰色地带"做语义分析，回答"规则说没问题，但实际到底好不好"。

### 2.1 哪些 session 需要 LLM 裁判？

**核心原则：只对规则判不准的 session 用 LLM。** 规则已经能明确给出黑白结论的 session（极好 / 极差），不需要 LLM 复审。

#### 需要 LLM 裁判的 4 类 session

| 类别 | 筛选条件 | 为什么需要 LLM | 该层抽样比例 |
|-----|---------|---------------|------------|
| **A. 灰色地带** | 健康度 50-69（风险档） | 规则知道有问题，但说不清"为什么空转"、"为什么截断"。需要语义分析根因 | **30%** |
| **B. 规则与异常标签矛盾** | 健康度 ≥ 70 但命中了异常标签<br>或 健康度 < 70 但无异常标签 | 规则内部信号打架，必须人工或 LLM 仲裁 | **50%** |
| **C. 高分可疑组** | 健康度 70-84（一般档）+ 无异常标签 | 分数不低但没到优秀，可能存在规则覆盖不到的语义质量问题 | **15%** |
| **D. 严重档根因分析** | 健康度 < 50 | 规则已能识别"有问题"，但无法说清根因。LLM 用来做**根因归类**而非完整 5 维评判 | **100%（仅根因）** |

#### 不需要 LLM 裁判的 session

| 类别 | 条件 | 理由 |
|-----|------|------|
| 规则确认健康 | 健康度 ≥ 85 + 无异常标签 | 规则已有高置信度结论，跑 LLM 性价比低 |
| 规则确认严重且根因明确 | 健康度 < 50 + 异常标签明确指向单一根因（如"上下文超限"） | 根因已自明，无需 LLM 重复判断 |

> **抽样数预估**（按本周 500 条 session 估算）：
>
> - A 层（约 50 条）抽 15 条
> - B 层（约 30 条）抽 15 条
> - C 层（约 150 条）抽 23 条
> - D 层（约 50 条）全部做根因分析
>
> **合计约 100 条，token 成本和耗时可控。**

> ⚠️ **抽样比例是初版拍定的数字，后续需要根据实际跑 1-2 周的结果校准。** 校准依据：
>
> 1. 如果某层 LLM 与规则结论高度一致（偏差 < 10 分），说明规则已经够用，可降低该层抽样比例
> 2. 如果某层频繁发现规则盲区，应提高抽样比例
> 3. 如果总成本超出预期，优先压缩 C 层（高分组）抽样

---

### 2.2 裁判模型选什么？

**选型原则**：选在长上下文推理 + 复杂逻辑判断方向表现最好的模型，不要用轻量模型。

**推荐顺序**：

1. **Claude 3.5 Sonnet / Claude 3 Opus** — 在长链路推理、轨迹理解、根因分析上表现最稳定，强烈推荐
2. **GPT-4o / GPT-4 Turbo** — 长上下文能力强，结构化输出稳定
3. **Gemini 1.5 Pro** — 上下文窗口长（适合极长轨迹）

**不推荐**：

- 轻量模型（GPT-4o-mini、Claude Haiku 等）— 评判 Agent 轨迹需要细粒度推理，轻量模型容易给出表面化结论
- 国产基础模型（除非已专门 fine-tune 过 Agent 评判任务）

> ⚠️ **建议先用 Claude 3.5 Sonnet 跑一批（约 100 条），人工抽 10 条对比 LLM 评判结果与人工判断的一致性。一致性 ≥ 80% 才能继续使用，否则换模型或调整 Prompt。**

---

### 2.3 LLM 评什么？

围绕平台 5 个维度，LLM 裁判做**规则做不到的语义判断**：

| 维度 | 规则能判的 | LLM 补位什么 | LLM 评判标准 |
|------|-----------|------------|------------|
| **思考** | 有无答复、是否截断、是否空转 | 空转的步骤在"想什么"？是有效推理还是兜圈子？ | 逐步阅读 reasoning 内容。**有效推理 = 每步产生新信息或决策**；兜圈子 = 重复已有结论或无实质进展 |
| **编排** | 连续重试次数、工具多样性 | 重试是参数微调（合理）还是盲目重试（不合理）？工具选择是否最优？ | 比对相邻步骤的工具调用参数变化。**参数有调整 = 有策略重试**；参数不变 = 盲目重试 |
| **稳定** | 最终成功率 | 失败后的恢复策略是否合理？是换方案还是死磕？ | 失败后下一步行为：**换工具/换参数 = 合理恢复**；原样重试 = 死磕 |
| **响应** | 总耗时 | 耗时长是任务复杂（合理）还是低效循环（不合理）？ | 区分必要耗时与浪费耗时。**有效步骤占比 ≥ 70% = 合理耗时** |
| **资源** | Token 总量 | Token 花在有效推理还是冗余上下文？ | 检查上下文是否含大段无关历史。**有效信息占比 ≥ 60% = 合理消耗** |

---

### 2.4 LLM Prompt 模板

```
你是一个 Agent 执行质量评审员。以下是某次 Agent 会话的完整执行轨迹。

[会话元信息]
- 任务：{user_query}
- 总步数：{total_steps}
- 规则健康度：{health_score}
  （思考{thinking}/编排{orch}/稳定{stability}/响应{response}/资源{resource}）

[执行轨迹]
Step 1: reasoning="..." → tool_call(name="...", args="...") → result="..."
Step 2: ...
...

请按以下维度逐一评判（每个维度需给出：分数 + 一句话理由 + 最关键的问题点）：

1. 思考质量（0-100）：每步推理是否推进了任务目标？有无兜圈子？
2. 编排质量（0-100）：工具选择是否合理？重试是否有策略？有无可合并/省略的步骤？
3. 恢复能力（0-100）：失败后是否调整策略？恢复是否有效？
4. 资源效率（0-100）：Token 是否花在有效信息上？有无冗余上下文？
5. 综合判断（0-100）：整体执行质量

最后请输出：
- 主要问题（一句话）
- 根因分类（Prompt / 工具 / 规划 / 环境 四选一）
- 改进建议（一条具体可执行的动作）
```

---

### 2.5 专题 · 效率维度（合并响应）的三层评估方案

> **改动说明**：原"响应"维度判定标准不合理（60s 一刀切、把任务难度和执行质量混在一起）。本次重构将原"响应"维度合并改造为**「效率」维度**——用户体感（等多久）和执行质量（耗时是否该花）都属于时间问题，合并到一个维度更准确。
>
> **三层评估顺序**：规则评（全量） → LLM 评（抽样） → 人评（精筛）。本节按这个顺序拆解效率维度的判定逻辑。

---

#### A. 规则层能算什么（结构信号）

**规则不读内容，只数行为**。规则只能回答 What（哪一步慢、总共多久），回答不了 Why（这一步该不该存在）。

##### A.1 用户体感分（绝对耗时，按任务难度分桶）

放弃 60s 一刀切，按 `tool_calls` 数分桶给阈值：

| 任务类型 | 判定条件 | 满分阈值 | 触底阈值（30 分） |
|---------|---------|---------|-----------------|
| 简单任务 | `tool_calls < 3` | ≤ 30s | 90s |
| 中等任务 | `3 ≤ tool_calls < 10` | ≤ 90s | 240s |
| 复杂任务 | `tool_calls ≥ 10` | ≤ 240s | 600s |

公式：

```
体感分 = 100 × (1 − max(0, duration − 满分阈值) / (触底阈值 − 满分阈值))
        ↓ 范围裁剪
        ∈ [30, 100]，调用整体失败时硬封顶 50
```

##### A.2 规则效率分（结构代理信号）

不依赖 LLM，仅靠轨迹结构数得出来的信号：

| 检测项 | 命中规则 | 扣分 | 数据来源 |
|--------|---------|-----|---------|
| **环境黑洞** `env_dead_end` | 同 tool + 同 args 连续调用 ≥ 3 次 | **-20** | 已有 `max_serial_run` 信号 |
| **重复编辑** `repetitive_edit` | 同一文件路径修改次数 ≥ 3 | **-20** | `edit_file(path=A)` 出现次数 |
| **思考冗余** `thinking_redundancy` | 单步 thinking token > 全体 P95 | **-10** | 每个 LLM span 的 thinking 长度 |
| **规划主动性缺失** `planning_behavior` | 中等及以上任务，前 3 步 thinking 未命中 `Plan:` / `步骤[:：]` / `1\.|2\.` / `首先` 关键词 | **-10** | thinking 文本正则 |
| **规划遵循度（弱代理）** `plan_adherence` | 声明 N 步计划，但实际执行步数 < N 或 > 1.5N | **-10** | 关键词解析 + 实际 step 计数 |
| **测试残留** `test_residues` | 全程出现 `mkdir` / `touch` / `write_file(临时)`，但最后 3 步未出现 `rm` / `delete` / `cleanup` | **-5** | 工具调用序列尾部检测 |

公式：

```
规则效率分 = 100 − Σ(命中扣分)，下限 0
```

> ⚠️ **规则代理信号必然有误报**（数对了行为但语义可能合理），所以扣分权重比纯 LLM 版本保守。规则效率分**不是终审**，是为了在没有 LLM 的情况下提供一个可用的代理。

##### A.3 效率总分（规则版）

```
效率总分 = 0.4 × 体感分 + 0.6 × 规则效率分
```

- 权重偏向"规则效率分"，因为体感分容易被任务难度干扰
- 这是**纯规则可计算**的版本，全量 session 都能算

---

#### B. LLM 层校准（抽样）

LLM 不重新算分，而是**校准规则代理的误报和漏报**。

##### B.1 LLM 评什么

对阶段二筛选出来的约 100 条 session，针对效率维度的 6 项子规则做语义判定（`plan_adherence` 已由 LLM 主评，因规则代理太弱）：

| LLM 判定的 6 项子规则 | 干嘛 |
|--------------------|------|
| `thinking_redundancy` 思考冗余 | 判断 thinking 长内容是必要推理还是无效展开 |
| `planning_behavior` 规划主动性 | 判断是否有可识别的全局规划意识 |
| `plan_adherence` 规划遵循度 | 把"声明的计划"和"实际执行"做语义对齐 |
| `repetitive_edit` 重复编辑 | 判断同文件多次修改是迭代收敛还是反复回退 |
| `env_dead_end` 环境黑洞 | 判断重复探查是有信息增量还是纯死路 |
| `test_residues` 测试残留 | 判断残留物是任务产物还是临时文件 |

##### B.2 校准动作

每条 LLM 评判结果与规则代理结果做交叉：

| 规则判定 | LLM 判定 | 处理 |
|---------|---------|------|
| 命中 | 命中 | 规则准确，保留扣分 |
| 命中 | 未命中 | **规则误报**，进入校准库（可能调整阈值） |
| 未命中 | 命中 | **规则漏报**，进入校准库（可能补规则） |
| 未命中 | 未命中 | 规则准确，无操作 |

> 跑满 2 周后统计**误报率 / 漏报率**，作为下一轮规则迭代的输入。

##### B.3 LLM SP（System Prompt）

```
你是 Agent 执行效率评审员。规则系统已经告诉你"用户等了多久"和"哪些工具
被反复调用"，但规则无法判断这次执行的耗时是否"该花"。

你的任务是：从 6 个子项判断 Agent 是否存在"无效耗时"，输出每项 0/1
判定 + 关键证据 + 整体效率分（0-100）。

[输入]
- 任务：{user_query}
- 规则给出的耗时：{duration}s（关键路径 {cp_duration}s，占比 {cp_ratio}%）
- 规则代理已命中：{rule_hit_list}
- 完整执行轨迹（thinking / tool_call / tool_result 逐步）：{trajectory}

[判定规则]
对以下 6 个子项各自输出 hit ∈ {0, 1}（1 = 命中该问题）+ evidence：

1. thinking_redundancy（思考冗余）
   - 1：≥1 处 thinking 段落包含与下一步执行无关的展开（如"假如…就…"
     的假设链 ≥3 层、未被采用的方案完整描述、提前粘贴整段代码）
   - 0：所有 thinking 都直接服务于下一步决策

2. planning_behavior（规划主动性）
   - 1：任务复杂度 ≥ 中等，但 Agent 没有在前 3 步内给出可识别的全局
     计划（无 "Plan:" / "步骤：1. 2. 3." 等结构化规划文本）
   - 0：有清晰可识别的全局规划

3. plan_adherence（规划遵循度）
   - 1：声明了计划但实际执行偏离 ≥ 1 个关键节点（跳过 / 顺序错乱 /
     增加未声明步骤）且无显式调整说明
   - 0：无规划 或 计划与执行一致 或 调整有显式说明

4. repetitive_edit（重复编辑）
   - 1：对同一文件 / 同一函数 / 同一段内容修改 ≥ 3 次，且后续修改
     有"撤回前次"的特征（diff 显示改回原状或在两个版本间反复）
   - 0：不存在反复修改

5. env_dead_end（环境黑洞）
   - 1：连续 ≥ 3 步对同一环境信号探查（同一文件读取 / 同一命令执行 /
     同一接口查询），且每次返回信息无新增、Agent 决策无收敛
   - 0：探查行为有收敛

6. test_residues（测试残留）
   - 1：任务完成后存在未清理的临时文件 / 调试 print / mock 数据，
     且 Agent 未在最后步骤显式清理
   - 0：无残留 或 已显式清理

[输出格式]
{
  "thinking_redundancy":   { "hit": 0/1, "evidence": "Step N: ..." },
  "planning_behavior":     { "hit": 0/1, "evidence": "..." },
  "plan_adherence":        { "hit": 0/1, "evidence": "..." },
  "repetitive_edit":       { "hit": 0/1, "evidence": "..." },
  "env_dead_end":          { "hit": 0/1, "evidence": "..." },
  "test_residues":         { "hit": 0/1, "evidence": "..." },
  "llm_efficiency_score":  0-100,
  "primary_waste":         "命中项中影响最大的一个 key",
  "improvement_action":    "一条具体可执行的优化动作"
}
```

---

#### C. 人评层（仲裁）

LLM 与规则严重分歧的 case 进入人评：

- 规则效率分与 LLM 效率分偏差 ≥ 15
- 6 项子规则中规则与 LLM 判定不一致 ≥ 2 项
- LLM 评分 < 50 但规则评分 > 70（潜在规则盲区）

人评做的事：

1. 仲裁哪一方判断更准
2. 把误报/漏报的 case 写入校准库
3. 对真问题定根因（Prompt / 工具 / 规划 / 环境）

---

#### D. 三层评估结果合并

每条 session 最终的效率分计算逻辑：

```
IF 有人评结果：
    效率分 = 人评分（最终权威）
ELIF 有 LLM 评结果：
    效率分 = 0.4 × 体感分 + 0.6 × LLM 效率分
ELSE：
    效率分 = 0.4 × 体感分 + 0.6 × 规则效率分（默认）
```

---

#### E. 子项与平台改造建议

| 子项 | 规则代理 | LLM 评判 | 是否进入 P0 过滤 | 备注 |
|------|---------|---------|-----------------|------|
| `env_dead_end` | ✅ | ✅ | P0 候选 | 规则准、影响大 |
| `repetitive_edit` | ✅ | ✅ | P0 候选 | 规则准、影响大 |
| `thinking_redundancy` | ⚠️ 粗代理 | ✅ | P0 候选 | 截图标"PO 过滤？？"，需 1 周跑批后确认 |
| `planning_behavior` | ⚠️ 关键词代理 | ✅ | 自迭代 | 不做硬过滤 |
| `plan_adherence` | ⚠️ 弱代理 | ✅ | 自迭代 | 规则精度低，主以 LLM 为准 |
| `test_residues` | ⚠️ 粗代理 | ✅ | 检测即可 | 仅作清单提示，不影响打分核心 |

---

> ⚠️ **本节所有阈值都是初版拍定**，包括：
>
> - 任务难度分桶：tool_calls 3 / 10
> - 体感分阈值：30s/90s/240s
> - 规则代理扣分权重：-20/-10/-5
> - 规则代理判定阈值（连续 3 次、修改 3 次、token P95 等）
> - 三层合并的权重 0.4/0.6
>
> 第 1 周跑批完成后必须做两件事：
>
> 1. 人工抽样 10 条对比 LLM 一致性，要求 ≥ 80%
> 2. 统计规则代理的误报率 / 漏报率，**误报率 < 20% 才允许进入官方健康度计算**，否则规则效率分仅作参考分展示

---

### 2.6 输出物：问题清单

| Session ID | 规则健康度 | LLM 综合分 | 偏差 | 主要问题 | 根因分类 |
|-----------|-----------|-----------|------|---------|---------|
| af2cb1 | 49 | 35 | -14 | 空转 5 步兜圈子，未产出答复 | Prompt |
| 441ce4 | 62 | 58 | -4 | 连续 7 次重试同一工具，参数未调整 | 工具 |
| b3d2f7 | 78 | 65 | -13 | 规则判一般，实际推理偏离目标 | 规划 |

> **偏差列很重要**：规则分和 LLM 分差距大的 session，是规则覆盖不到的盲区，也是平台规则需要迭代的方向。

---

## 阶段三 · 人工校准（Day 4-5）

**目标**：从 LLM 裁判结果中，挑出最需要人看的 session，做最终定性。

### 3.1 哪些 session 需要人工审阅？

| 挑选条件 | 说明 | 预计数量 |
|---------|------|---------|
| **LLM 与规则偏差 ≥ 15 分** | 规则和 LLM 判定严重不一致，需要人做仲裁 | 约 10-15 条 |
| **LLM 综合分 < 50** | LLM 认为严重问题，需要人确认根因和影响面 | 约 10 条 |
| **影响面大的异常类型代表** | 阶段一识别的命中数 Top 3 异常类型，每类挑 1 条代表深度分析 | 约 3-5 条 |
| **随机抽 3 条 LLM 综合分 > 85** | 验证 LLM 是否误判（高分但实际差），用于校准 LLM 可信度 | 3 条 |

> **总量预估约 25-35 条**，每条人工审阅约 5-8 分钟，总计 2-3 小时可完成。

> ⚠️ **挑选规则中的数字（偏差 15 分、综合分 50、Top 3、随机 3 条）都是初版拍定的。** 后续需要根据实际数据校准：
>
> - 如果偏差普遍 < 10 分 → 说明规则已经够准，LLM ROI 不高，可降低 LLM 抽样
> - 如果偏差普遍 > 20 分 → 说明规则盲区很大，应优先迭代规则而非依赖 LLM
> - 校准节奏建议：第 1 周跑完后 review 一次，第 4 周再 review 一次

---

### 3.2 人工审阅做什么？

1. **打开 session-detail 页**，逐步回放执行轨迹
2. **确认 LLM 裁判判断是否准确**（是 / 否 / 部分准确）
3. **定性根因**，从以下 4 类中选择：

| 根因类别 | 表现 | 优化方向 |
|---------|------|---------|
| **Prompt 问题** | 指令不清晰 / 缺乏约束 / 缺乏示例 | 改 system prompt / 加 few-shot |
| **工具问题** | 工具返回格式差 / 工具不稳定 / 工具能力不足 | 改工具实现 / 加重试退避 / 加备用路由 |
| **规划问题** | Agent 任务分解差 / 步骤拆解不合理 | 改规划 prompt / 引入 Plan-then-Execute |
| **环境问题** | 模型能力不足 / 上下文窗口限制 / 下游服务不稳 | 换模型 / 优化上下文管理 / 推动下游修复 |

4. **写出可执行的优化动作**

---

### 3.3 最终输出：周评报告

| 优先级 | 问题 | 影响面 | 根因 | 优化动作 | 负责人 | 验证方式 |
|-------|------|-------|------|---------|-------|---------|
| P0 | 18% session 无答复退出 | 42 条/周 | Prompt 缺乏"必须产出最终答复"约束 | system prompt 末尾增加强制输出指令 | @xxx | 下周无答复率 < 5% |
| P1 | 12% session 死循环重试 | 27 条/周 | 工具返回格式不清晰，Agent 无法理解结果 | 规范工具 output 为 JSON + 增加 result_schema | @xxx | 下周死循环率 < 3% |
| P2 | 6% session 执行失败 | 15 条/周 | 下游服务偶发超时 | 增加重试退避 + 备用工具路由 | @xxx | 下周失败率 < 2% |

---

## 五天时间安排

| 日期 | 阶段 | 主要工作 | 预计耗时 |
|------|------|---------|---------|
| **Day 1（周一）** | 阶段一 | 规则扫描 → 输出问题分布表 | 1 小时 |
| **Day 2（周二）** | 阶段一 → 阶段二 | 完成筛选 → 启动 LLM 裁判跑批 | 0.5 小时 + 跑批等待 |
| **Day 3（周三）** | 阶段二 | LLM 跑批结果 review → 输出问题清单 | 2 小时 |
| **Day 4（周四）** | 阶段二 → 阶段三 | 完成 LLM 评判收尾 → 启动人工审阅 | 2 小时 |
| **Day 5（周五）** | 阶段三 | 完成人工审阅 → 输出周评报告 → 周会同步 | 3 小时 |

---

## 关键数字总览（均为初版，后续校准）

| 数字 | 当前值 | 校准依据 |
|------|-------|---------|
| LLM 裁判 A 层抽样比例 | 30% | 跑 1-2 周后看 LLM 与规则的偏差分布 |
| LLM 裁判 B 层抽样比例 | 50% | 同上 |
| LLM 裁判 C 层抽样比例 | 15% | 如果该层很少发现新问题，可下调到 5% |
| LLM 裁判 D 层抽样比例 | 100% | 严重档样本量小，建议保持全量 |
| 人工审阅偏差阈值 | 15 分 | 跑 4 周后统计偏差分布 P50/P75 调整 |
| 人工审阅 LLM 综合分阈值 | < 50 | 同上 |
| 人工审阅总量 | 25-35 条/周 | 如果实际审阅时间 > 3 小时，应压缩抽样 |

---

## 附录 · 常见问题

**Q1：如果某周 session 总量翻倍，抽样规则需要调整吗？**
保持比例不变即可。如果绝对量超过预算（如 LLM 跑批超过 200 条），优先压缩 C 层（高分组）抽样比例。

**Q2：LLM 裁判和人工判断不一致怎么办？**
以人工判断为准。不一致的 case 应记录下来，作为后续优化 LLM Prompt 的素材。

**Q3：周评报告的 Action 没人做完怎么办？**
未完成的 Action 进入下周评审表，并标注"延期次数"。延期 ≥ 2 次的 Action 需要在周会上重点同步。

**Q4：规则和 LLM 都判不出来的问题怎么发现？**
依赖人工随机抽样（阶段三 3.1 中的"随机抽 3 条 LLM 综合分 > 85"）。如果在这批中频繁发现问题，说明规则和 LLM 都有盲区，需要迭代评估体系本身。
